# Claude Code Instructions

## Version Management

- **Do NOT modify the `VERSION` file.** The DSL version is updated automatically by CI/CD pipelines. Never bump, change, or set the version manually.
- The `meta-dsl` dependency version in `gradle/libs.versions.toml` can be updated as needed — that is a dependency, not the project version.
